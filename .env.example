# Where the aiacct FastAPI backend is listening.
# The browser never talks to this directly - every call goes through the Next.js
# BFF at /api/v1/*, which injects the bearer token from an httpOnly cookie.
# The backend has no CORS middleware, so a direct browser call would fail.
AIACCT_API_URL=http://localhost:8000
